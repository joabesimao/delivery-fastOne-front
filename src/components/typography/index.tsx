import { Component, type ReactNode } from "react"
type TypographyProps = {
  children: ReactNode;
};

export const  Typography = ({children}:TypographyProps) => {
    return (
        <h1>Procurar um Cliente</h1>
    )
}